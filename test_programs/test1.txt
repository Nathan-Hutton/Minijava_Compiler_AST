// test.minijava

/*
 * (Note: a line in main() refers to this class, "test":
 *     test temp = this;
 * If you save this program as something other than
 * test.minijava, you will need to update that line
 * in order for this to compile.
 */

/*
 * Input: 212
 * Output: 212.0°F is 100.0°C
 *
 * Input: -40
 * Output: -40.0°F is -40.0°C
 */

import java.util.Scanner;
import java.lang.reflect.*;

double FACTOR = 5.0 / 9;
double OFFSET = 32;

double FtoC(double F) {
    double C = this.FACTOR * (F - OFFSET);
    return C;
}

String toString() {
    return "This is a Temp object!";
}

main() {
    Scanner in = new Scanner(System.in);
    System.out.print("Enter a temperature in Fahrenheit (°F): ");
    double fahr = in.nextDouble();

    test1 temp = this;
    double cels = temp.FtoC(fahr);

    System.out.println(fahr + "°F is " + cels + "°C");
}